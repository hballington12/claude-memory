Clone the repo:
- git clone git@github.com:hballington12/claude-memory.git

Install the tool:
- cd claude-memory
- uv pip install -e .

Init the tool in your favourite projects:
- skills init

Restart claude in the project and watch the `.claude/skills/project-memory` evolve as you work.
